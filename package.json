{
    "name": "bno08x",
    "version": "0.0.5",
    "description": "I2C driver for BNO08x IMU.",
    "main": "dist/index.js",
    "types": "dist/types.d.ts",
    "scripts": {
        "test": "jest --runInBand",
        "debug-build": "node-gyp rebuild --debug && tsc",
        "configure": "node-gyp configure",
        "build": "node-gyp rebuild && tsc",
        "install": "node-gyp rebuild && tsc",
        "bear-build": "bear -- node-gyp build && tsc",
        "rebuild": "node-gyp rebuild && tsc",
        "bear-rebuild": "bear -- node-gyp rebuild && tsc",
        "server": "node-gyp build && tsc && tsc -p tsconfig.esm.json && cp client.html dist/client.html && cp -r node_modules/three/build/*.js dist/ && node dist/server.js",
        "visual": "node-gyp build && tsc && tsc -p tsconfig.esm.json && cp visual.html dist/visual.html && cp -r node_modules/three/build/*.js dist/ && node dist/visual.js",
        "clean": "node-gyp clean"
    },
    "keywords": [
        "BNO08x",
        "OpenI2C"
    ],
    "author": "",
    "license": "MIT",
    "gypfile": true,
    "devDependencies": {
        "jest": "^29.7.0",
        "ts-jest": "^29.3.2"
    },
    "dependencies": {
        "@types/jest": "^29.5.14",
        "@types/node": "^22.15.17",
        "@types/three": "^0.178.1",
        "@types/ws": "^8.18.1",
        "@types/express": "^5.0.3",
        "bindings": "^1.5.0",
        "ts-node": "^10.9.2",
        "typescript": "^5.8.3",
        "express": "^5.1.0",
        "three": "^0.178.0",
        "ws": "^8.18.3"
    }
}